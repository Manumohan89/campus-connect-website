# ─── Local Development ─────────────────────────────────────────────────────
# Uses React proxy (package.json "proxy" field) → no REACT_APP_API_URL needed
# REACT_APP_API_URL=http://localhost:5000/api
